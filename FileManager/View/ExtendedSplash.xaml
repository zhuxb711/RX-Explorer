<Page
    x:Class="FileManager.ExtendedSplash"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FileManager"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    NavigationCacheMode="Disabled"
    Background="{ThemeResource SystemAccentColor}"
    mc:Ignorable="d">

    <Grid>
        <Canvas>
            <Canvas.ChildrenTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition IsStaggeringEnabled="True" FromVerticalOffset="200"/>
                </TransitionCollection>
            </Canvas.ChildrenTransitions>
            <Image Name="extendedSplashImage" Source="ms-appx:///Assets/SplashScreen.png"/>
            <TextBlock FontSize="16" Name="Display" TextAlignment="Center" Width="350" Height="50"/>
            <StackPanel Orientation="Horizontal" Name="ButtonPane" Visibility="Collapsed" Width="210">
                <Button Name="NavigationButton" Content="授予权限" HorizontalAlignment="Center" FontSize="16" Width="100" Click="NavigationButton_Click"/>
                <Button Name="CloseButton" Content="关闭应用" Margin="10,0,0,0" HorizontalAlignment="Center" FontSize="16" Width="100" Click="CloseButton_Click"/>
            </StackPanel>
        </Canvas>
    </Grid>
</Page>
